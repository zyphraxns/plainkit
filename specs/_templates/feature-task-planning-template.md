# {{功能名称}} — 开发任务计划

## 1. 任务概览

**总任务数**：{{N}} 个
**预计总工时**：{{X}} 分钟（约 {{Y}} 小时）
**开发方法**：TDD — 每个任务按 RED → GREEN → REFACTOR 循环执行

**关键标注**：
- 🔒 阻塞任务：被多个任务依赖，建议优先完成
- ⚠️ 风险任务：技术难度高，可能需要额外时间

### 依赖关系图

```mermaid
graph LR
    %% 根据实际任务生成，标注阻塞任务（红色）和风险任务（橙色）
    {{依赖关系图}}
```

### 可并行任务组

| 并行组 | 任务 | 说明 |
|--------|------|------|
| {{组号}} | {{任务编号}} | {{为什么可以并行}} |

---

## 2. 开发任务

> 按垂直切片组织。每个阶段对应一个可独立运行和验证的用户行为（加可选的基础设施层）。切片内部的任务按技术层自然顺序排列。
>
> 每个任务按 TDD 循环执行：RED（根据验证标准写测试）→ GREEN（写最小实现通过测试）→ REFACTOR（重构）

### {{阶段名称（以用户行为命名）}}

**阶段完成标准**：{{这个切片做完后，用户可以执行什么完整行为}}

---

#### Task-{{XX}}: {{任务标题}} {{🔒/⚠️ 如有}}

**通俗解释**：{{一句不含技术术语的话，说明做完后用户或系统会有什么可感知的变化}}

**做什么**：{{具体要做的事情}}

**涉及文件**：`{{文件路径}}`

**参考**：技术方案 {{章节}} → AC-{{XXX}}

**依赖**：{{前置任务编号，无则写"无"}}

**预估工时**：{{N}} 分钟

**验证标准**（TDD RED 阶段直接转化为测试用例）：
- [ ] {{具体的输入 → 预期输出，能直接写成测试断言}}
- [ ] {{覆盖正常情况}}
- [ ] {{覆盖边界情况}}
- [ ] {{覆盖异常情况}}

---

{{更多任务按上面的格式继续，更多阶段按需添加}}

---

## 3. AC 覆盖总表

> 最终检查：每条 AC 是否都有任务承接。这是全文档唯一的 AC 映射汇总。

| AC 编号 | 验收标准概述 | 承接任务 | 验证方式 |
|---------|-------------|---------|---------|
| AC-{{XXX}} | {{概述}} | Task-{{XX}} | {{如何验证这条 AC 已被满足}} |

---

## 4. 完成定义

> 所有任务完成后，功能整体交付前的最终确认。只列出跟这个功能相关的检查项，不要套用通用清单。

- [ ] 所有任务的验证标准（测试用例）通过
- [ ] AC 覆盖总表中每条 AC 的验证方式已执行并通过
- [ ] {{跟这个功能相关的特定检查项，如：数据库迁移脚本在测试环境验证通过}}
- [ ] {{跟这个功能相关的特定检查项，如：真实接口联调通过（如涉及 Mock 替换）}}