# {{功能名称}} — 技术设计文档

## 1. 设计概要

**功能描述**：{{一句话描述}}

**影响范围**：{{涉及的模块，如：用户模块、权限模块、订单模块}}

**技术难点**：{{关键技术挑战，没有则写"无"}}

**外部依赖**：{{是否依赖其他功能或外部服务，没有则写"无"}}

---

## 2. 架构概览

> 根据功能复杂度选择合适的描述方式：
>
> **简单功能**（单模块、单接口的 CRUD）：用 2-3 句话说清"谁调谁、数据往哪走"即可，不需要画图。
>
> **中等功能**（跨 2-3 个模块）：用一段文字描述模块交互 + 一张 Mermaid 时序图展示关键链路。
>
> **复杂功能**（跨多模块、涉及异步/状态机/外部服务）：模块职责表 + Mermaid 时序图 + 数据流说明，必要时拆成多张图。

{{架构描述}}

---

## 3. 数据库设计

### 新增表

#### `{{table_name}}`

**用途**：{{业务含义}}

| 字段名        | 类型        | 约束                        | 说明     |
| ---------- | --------- | ------------------------- | ------ |
| id         | BIGINT    | PK, AUTO_INCREMENT        | 主键     |
| {{field}}  | {{type}}  | {{constraints}}           | {{说明}} |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间   |

**索引**：{{索引设计及理由}}

```sql
-- 建表语句
{{SQL}}
```

### 修改现有表（如有）

#### `{{existing_table}}`

**变更内容**：{{改了什么，为什么改}}

```sql
{{变更 SQL}}
```

**数据迁移**：{{是否需要迁移脚本，没有则写"无需迁移"}}

---

## 4. API 设计

> 遵循项目已有的 API 风格。每个接口的异常处理直接写在接口详情中，不单独成节。

### `{{METHOD}} {{/path}}`

**描述**：{{接口功能}} → AC-{{XXX}}

**鉴权**：{{是否需要登录/权限}}

**Request**：
```json
{{请求体}}
```

**Response（成功）**：
```json
{{响应体}}
```

**异常响应**：

| 场景 | 状态码 | 响应 | 对应 AC |
|------|--------|------|---------|
| {{参数校验失败}} | 400 | {{错误信息}} | AC-{{XXX}} |
| {{权限不足}} | 403 | {{错误信息}} | AC-{{XXX}} |

---

{{更多接口按上面的格式继续}}

---

## 5. 核心逻辑

> 描述不能单从 API 定义看出来的关键业务逻辑。简单的 CRUD 不需要这一节。

### {{逻辑名称}} → AC-{{XXX}}

**触发条件**：{{什么时候执行}}

**处理流程**：
1. {{步骤 1}}
2. {{步骤 2}}
3. {{步骤 3}}

**伪代码**（复杂逻辑才需要）：
```
{{伪代码}}
```

**状态流转**（如果涉及）：
```mermaid
stateDiagram-v2
    {{状态图}}
```

---

## 6. 现有代码改动

> 这次设计会动到哪些现有代码？每处改动的原因是什么？

| 模块 / 文件 | 改动内容 | 原因 | 对应 AC |
|-------------|---------|------|---------|
| {{模块名}} | {{改了什么}} | {{为什么要改}} | AC-{{XXX}} |

---

## 7. 技术决策

> 只记录**真正纠结过**的选择。判断标准：如果另一个方案也说得通、换个人可能会选它，那就值得记录。反过来，如果选择是显而易见的（用项目已有的技术栈、遵循已有的设计模式），不需要记录。
>
> 如果这次设计没有任何需要取舍的决策，删掉整个第 7 节。

### {{决策主题}}

**背景**：{{遇到了什么问题，为什么需要做选择}}

**选项**：
- A: {{方案 A}} — {{优势和代价}}
- B: {{方案 B}} — {{优势和代价}}

**结论**：{{选了哪个，一句话说明核心理由}}

---

## 8. 安全与性能（按需）

> 以下各项只在该功能确实涉及时才写。不涉及的直接跳过，不要写"无"占位。

**输入校验**：{{如果有超出 API 层基本校验的额外校验逻辑}}

**缓存策略**：{{如果引入了缓存}}

**限流策略**：{{如果需要限流}}

**敏感数据处理**：{{如果涉及密码、支付、个人信息等}}

**性能考量**：{{如果有明确的性能要求或潜在瓶颈}}

---

## 9. AC 覆盖总表

> 最终检查：每条 AC 是否都有着落。这是全文档唯一的 AC 映射汇总，其他章节通过 `→ AC-XXX` 行内标注。

| AC 编号 | 验收标准概述 | 实现位置 |
|---------|-------------|---------|
| AC-001 | {{概述}} | {{如：API POST /api/upload + 前端校验}} |
| AC-002 | {{概述}} | {{如：核心逻辑 5.1 + 异常响应 4.2}} |

---

## 附录：变更记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| {{YYYY-MM-DD}} | 初始版本 | — |