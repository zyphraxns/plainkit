# {{功能名称}} — {{切片名称}} 完成报告

**切片（阶段）**：{{切片名称，以用户行为命名，如"发表评论"、"评论列表"}}
**完成时间**：{{YYYY-MM-DD HH:mm}}
**切片完成标准**：{{用户可以执行什么完整行为，如"用户可以在页面上输入评论并提交，看到成功提示"}}

---

## 完成的任务

| 任务 | 测试 | 验收方式 | 状态 |
|------|------|---------|------|
| Task-{{XX}}: {{标题}} | {{N}} 个测试全部通过 | {{TDD / 手动验收}} | ✅ |
| Task-{{XX}}: {{标题}} | {{N}} 个测试全部通过 | {{TDD / 手动验收}} | ✅ |

---

## 手动验收汇总（如有）

> 本切片中涉及 UI 变化的任务的手动验收结果汇总。如果本切片没有 UI 任务，删掉这一节。

| 任务 | 验收清单 | 结果 | 期望调整 |
|------|---------|------|---------|
| Task-{{XX}} | `docs/验收清单/{{功能名}}_Task-{{XX}}_验收清单.md` | ✅ 全部通过 | ⬜ 无 / ⬜ 有 |

**期望调整汇总**：⬜ 无 / ⬜ 有 → 待后续 feature-evolution 处理

---

## 文件变更

### 新增
- `{{path}}` — {{一句话说明}}

### 修改
- `{{path}}` — {{改了什么}}

### 删除（如有）
- `{{path}}` — {{为什么删}}

---

## AC 覆盖进度

| AC 编号 | 状态 | 说明 |
|---------|------|------|
| AC-{{XXX}} | ✅ 已满足 | 由 Task-{{XX}} 实现并测试通过 |
| AC-{{XXX}} | ⏳ 待后续切片 | 需要 {{切片名称}} 完成 |

---

## 问题与决策（如有）

> 只记录开发过程中遇到的意外情况、做出的非显而易见的决策、以及对后续切片有影响的事项。如果一切顺利，这一节可以写"无"。

### {{问题/决策主题}}

**情况**：{{发生了什么}}
**处理**：{{怎么解决的}}
**影响**：{{对后续有什么影响，没有则不写}}

---

## 技术债务（如有）

> 开发过程中有意识跳过的优化、临时方案、已知但暂不处理的问题。没有就删掉这一节。

- {{描述}} — 建议在 {{什么时候}} 处理