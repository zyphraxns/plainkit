# {{功能名称}} — 变更记录 CR-{{序号}}

## 1. 变更概览

**变更标题**：{{简要描述}}
**变更类型**：{{微调 / 扩展 / 重构}}
**变更原因**：{{为什么要做这个变更}}
**发起日期**：{{YYYY-MM-DD}}
**关联功能**：{{原功能名称}}

---

## 2. 影响分析

> 只列出受影响的部分。不受影响的层面不需要写"无影响"来占位。

### 需求影响

| AC 编号 | 操作 | 说明 |
|---------|------|------|
| AC-{{新编号}} | 新增 | {{新验收标准概述}} |
| AC-{{已有编号}} | 修改 | {{原内容 → 新内容}} |
| AC-{{已有编号}} | 移除 | {{原因}} |

### 技术影响

| 影响范围 | 变更内容 |
|---------|---------|
| {{如：数据库}} | {{如：users 表新增 avatar 字段}} |
| {{如：API}} | {{如：新增 PUT /api/users/avatar}} |
| {{如：组件}} | {{如：修改 UserProfile 组件，增加头像上传}} |

### 代码与测试影响

| 文件 | 操作 | 说明 |
|------|------|------|
| `{{path}}` | {{新增/修改}} | {{具体改什么}} |
| `{{test path}}` | {{新增/修改/需调整}} | {{测试变更说明}} |

**回归风险**：{{哪些现有功能可能受影响，已有测试是否覆盖}}

---

## 3. 变更详情

### 新增/修改的验收标准

> Given-When-Then 格式，与需求文档保持一致。

- **AC-{{编号}}**: {{场景名称}}
  - **Given** {{前置条件}}
  - **When** {{用户操作}}
  - **Then** {{预期结果}}

### 技术变更（如涉及）

> 只写有变化的部分：数据库变更写 SQL，API 变更写接口定义，组件变更写设计说明。没有变化的层面跳过。

{{根据实际变更内容填写，不套用固定的"数据库/API/组件"三段式}}

### 兼容性说明（如涉及）

> 如果变更可能破坏现有数据或接口，说明迁移方案。不涉及兼容性问题则删掉这一节。

{{迁移步骤}}

---

## 4. 增量任务

> 任务编号从原任务规划最后一个编号之后继续。阶段根据实际变更内容划分，不套用固定结构。
>
> 每个任务按 TDD 循环执行：RED → GREEN → REFACTOR

### {{阶段名称}}

#### Task-{{N+X}}: {{任务标题}}

**通俗解释**：{{一句话说明做完后的可感知变化}}

**做什么**：{{具体内容}}

**涉及文件**：`{{path}}`

**参考**：本文档 {{章节}} → AC-{{XXX}}

**依赖**：{{前置任务编号，无则写"无"}}

**预估工时**：{{N}} 分钟

**验证标准**（TDD RED 阶段依据）：
- [ ] {{具体输入 → 预期输出}}
- [ ] {{边界/异常场景}}

---

{{更多任务和阶段按需添加}}

---

#### Task-{{最后}}: 回归验证

**做什么**：运行全量已有测试，确保变更未破坏原有功能

**依赖**：上述所有增量任务

**验证标准**：
- [ ] 原有测试全部通过
- [ ] 本次新增测试全部通过
- [ ] 测试覆盖率未下降

---

## 5. AC 覆盖总表

| AC 编号 | 操作 | 承接任务 | 验证方式 |
|---------|------|---------|---------|
| AC-{{XXX}} | {{新增/修改}} | Task-{{XX}} | {{如何验证}} |

---

## 6. 变更总结

**新增任务数**：{{N}} 个
**预计总工时**：{{X}} 分钟（约 {{Y}} 小时）
**风险等级**：{{低/中/高}} — {{风险说明，低风险可省略说明}}