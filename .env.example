# PlainKit 环境变量
#
# 本项目无后端、无数据库、无用户账号，因此【没有任何运行时环境变量】，
# 也不存在任何密钥。下面两个变量全部只在构建期使用。
#
# 禁止新增 PUBLIC_ 前缀的变量 —— Astro 会把 PUBLIC_* 注入到客户端代码，
# 而本项目没有任何需要出现在浏览器里的配置。见 specs/DESIGN.md §12。

# 部署基路径。
#   Cloudflare Pages（主站）: /
#   GitHub Pages（镜像）:     /<仓库名>/
BASE_PATH=/

# 托管平台构建环境使用的 Node 大版本，与 .nvmrc 保持一致。
NODE_VERSION=24
