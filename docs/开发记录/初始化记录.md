# 项目初始化记录

**执行时间**: 2026-09-21
**执行人**: AI Assistant (Role: DevOps / Scaffolder)
**Skill**: s7 项目初始化

---

## 1. 检查项

| 项 | 结果 |
|---|---|
| `specs/技术栈.md` 存在 | ✅ |
| `specs/项目结构.md` 存在 | ✅ |
| `specs/开发规范.md` 存在 | ⚠️ **文件名为 `specs/DESIGN.md`**，内容与 s5 模板要求的 15 章一致（另加设计系统与性能红线两章）。这是项目约定，不是缺失 |
| 目标目录状态 | ✅ 未初始化（无 `.git`、无 `package.json`），按「情况 A」执行 |
| 运行环境 | Node.js v24.17.0（与 `.nvmrc` 一致）；npm；git 可用；**Docker 未安装** |
| 网络 | npm registry 可达 |

---

## 2. 执行日志

### 2.1 核心初始化

| 命令 | 结果 |
|---|---|
| `git init` | ✅ 成功 |
| `git config user.name / user.email` | ✅ 仓库级身份设为 `liamqiu <193348236+zyphraxns@users.noreply.github.com>`（全局 git 身份未配置，用仓库级避免污染用户环境） |
| `npm install --save-dev astro typescript vitest prettier prettier-plugin-astro @astrojs/check @types/node` | ✅ 成功，293 个包，**0 漏洞** |
| `npx astro telemetry disable` | ✅ 已关闭（本项目立场是零追踪，构建环境同样） |

**最终依赖版本**

| 包 | 版本 |
|---|---|
| astro | 7.3.3 |
| typescript | **6.0.3**（不是 7.x，原因见下方「遗留问题」） |
| vitest | 5.0.1 |
| prettier | 3.9.8 |
| prettier-plugin-astro | 1.0.1 |
| @astrojs/check | 0.9.10 |
| @types/node | 已安装 |

`package.json` 的 **`dependencies` 为空 `{}`** —— 零运行时依赖这条硬规则已落实。

### 2.2 目录创建

按 `specs/项目结构.md` 的目录树创建，空目录均带 `.gitkeep`：

- [x] `src/layouts/`、`src/components/`、`src/styles/`
- [x] `src/lib/tools/`
- [x] `src/lib/shared/{date,url-state,canvas-card,format}/`
- [x] `src/pages/tools/`
- [x] `public/`
- [x] `scripts/`
- [x] `.github/workflows/`
- [x] `.vscode/`
- [x] `docs/开发记录/`、`docs/BUG修复文档/`（此前已建）
- [x] `specs/features/`（此前已建）

### 2.3 配置与源码文件

| 文件 | 状态 |
|---|---|
| `.gitignore` | ✅ |
| `.gitattributes` | 未创建（不需要：`.editorconfig` 已强制 LF，`.gitignore` 已排除二进制产物） |
| `README.md` | ✅ 含「如何启动」与「如何自托管」 |
| `.env.example` | ✅ 仅两个构建期变量（`BASE_PATH` / `NODE_VERSION`），**无任何密钥** |
| `package.json` + `package-lock.json` | ✅ |
| `astro.config.mjs` | ✅ |
| `tsconfig.json` | ✅ |
| `vitest.config.ts` | ✅ |
| `.prettierrc` / `.prettierignore` | ✅ |
| `.editorconfig` / `.nvmrc` | ✅ |
| `.vscode/settings.json` | ✅ |
| `src/styles/tokens.css` | ✅ 全部设计令牌 |
| `src/styles/base.css` | ✅ 重置 + 排版基线 + 外壳 + 工作台/读数/按钮 |
| `src/layouts/BaseLayout.astro` | ✅ 含 CSP、referrer、header/nav/footer |
| `src/pages/index.astro` | ✅ 首页索引（**零 JS**） |
| `src/pages/about.astro` | ✅ 隐私声明 + 自托管指引 |
| `src/lib/shared/result.ts` + `.test.ts` | ✅ `Result<T>` 判别式联合，含 4 个测试 |
| `src/lib/shared/tools.ts` | ✅ 已上线工具登记表（首页读它） |
| `public/favicon.svg` / `robots.txt` / `_headers` | ✅ |
| `scripts/check-budget.mjs` | ✅ 性能门禁 |
| `Dockerfile` / `nginx.conf` / `docker-compose.yml` | ✅ 已创建，**但未验证**（本机无 Docker） |
| `.github/workflows/ci.yml` / `deploy-pages.yml` | ✅ 已创建，**但未验证**（尚未推送） |
| `LICENSE` | ✅ Apache-2.0 官方全文（202 行 / 11,358 字节），附录版权行填 `Copyright 2026 liamqiu` |

### 2.4 验证（按 CI 完全相同的顺序）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1. 格式化 | `npm run format` | ✅ |
| 2. 格式检查 | `npm run format:check` | ✅ All matched files use Prettier code style |
| 3. 类型检查 | `npm run typecheck`（`astro check && tsc --noEmit`） | ✅ 0 errors / 0 warnings / 0 hints |
| 4. 测试 | `npm test` | ✅ 1 file / 4 tests passed |
| 5. 构建 | `npm run build` | ✅ 2 pages → `dist/` |
| 6. 性能门禁 | `npm run budget` | ✅ 全部通过 |

**性能门禁实测值**

```
  about/index.html   JS 0.0 KB   CSS 2.4 KB   HTML 3.4 KB（gzip 1.4 KB）
  首页 index.html     JS 0.0 KB   CSS 2.4 KB   HTML 2.1 KB（gzip 0.9 KB）
```

首页 JS **0 字节**、全站外部域名请求 **0** —— 两条硬红线在骨架阶段就已满足。

### 2.5 浏览器验收（真实渲染）

用本机已安装的 Google Chrome 无头模式打开真实页面并截图，覆盖：

- 首页：桌面 1280px / 移动 390px
- About 页：桌面 / 移动
- 设计系统预览：工具页模板在 760px（双列字段）与 390px（单列）下的真实渲染

**发现并修复的真实缺陷**（都不是靠单测能发现的）：

1. **说明区的第二个小节标题紧贴上一段** —— `base.css` 缺少 `.tool__notes-body + .tool__notes-title` 的上边距
2. **首页导航未标记当前项** —— `BaseLayout.astro` 里 `'/'` 归一化后变空字符串，与实际路径比较失败

---

## 3. 遗留问题 / TODO

### 2.6 Docker 自托管验证（补做）

Docker Desktop 装在 `/Users/liamqiu/Applications/Docker.app`（**用户级目录，不在 `/Applications`**），CLI 在 `/usr/local/bin/docker`。守护进程需先启动。

| 验证项 | 结果 |
|---|---|
| `docker compose up -d --build` | ✅ 构建 + 启动成功，25 秒 |
| 容器状态 | ✅ `Up`，`0.0.0.0:8080->80/tcp` |
| `http://localhost:8080/` | ✅ HTTP 200 |
| 目录式 URL `/about/` | ✅ HTTP 200（nginx `try_files` 生效） |
| 带哈希资源的长缓存头 | ✅ `Cache-Control: public, max-age=31536000, immutable` |
| 完整安全响应头 | ✅ 含 `frame-ancestors 'none'`（meta CSP 做不到的那条） |
| 未知路径 | ✅ HTTP 404 |
| 页面样式是否真的加载 | ✅ 截图确认，不是白板 |

镜像 `plainkit:latest`，93.1 MB。验证后已 `docker compose down` 关闭容器，镜像保留（下次 `docker compose up` 秒起）。

### 2.7 GitHub 仓库与线上部署（补做）

| 项 | 结果 |
|---|---|
| 仓库 | ✅ https://github.com/zyphraxns/plainkit （**Public**，默认分支 main） |
| 仓库标签 | ✅ tools / utilities / privacy / static-site / no-tracking / astro / open-source |
| CI 工作流 | ✅ success |
| Deploy to GitHub Pages | ✅ success |
| GitHub Pages 配置 | ✅ 通过 API 启用，`build_type=workflow` |
| 线上地址 | ✅ https://zyphraxns.github.io/plainkit/ |
| 线上 `/about/`、`/favicon.svg` | ✅ 均 HTTP 200 |

### 2.8 **线上验证抓到一个致命 bug（已修）**

首次部署后检查线上 HTML，发现：

```
href="/favicon.svg"                          ← 没有 /plainkit/ 前缀
href="/plainkit/_astro/BaseLayout.….css"     ← 有
```

**Astro 只会给「它自己生成的」资源自动加基路径，手写的 `href="/..."` 不会加。** 也就是说 GitHub Pages 镜像站上**导航链接会全部 404**——而这在本地 `npm run dev` 和主站上**完全看不见**。

**修复**：新增 `src/lib/shared/paths.ts`，导出 `href()`；所有手写的站内链接与 `public/` 静态资源引用一律经过它。

**防复发（两道防线）**：

1. `scripts/check-budget.mjs` 在 `BASE_PATH != '/'` 时检查所有根相对 `href` / `src` 是否带基路径前缀，不带即构建失败。**已做负向验证**：故意去掉前缀后门禁确实报错并给出可操作的修复提示
2. `.github/workflows/deploy-pages.yml` 用 `BASE_PATH=/<仓库名>/` 构建并跑门禁——主站构建发现不了这个问题，只有子路径构建才暴露

规则已写进 `specs/DESIGN.md` §5.7 与 §17 自检表。

---

## 3. 遗留问题 / TODO

### 3.1 仍未完成（不阻塞开发）

- [ ] **Cloudflare Pages 尚未配置**：需要在控制台建项目，构建命令 `npm run build`、输出目录 `dist`、环境变量 `NODE_VERSION=24`、`BASE_PATH=/`。
- [ ] **域名未领**（走 GitHub 学生包的 Name.com 额度，`.app` 后缀）。领到后要同步更新 `astro.config.mjs` 的 `site`。
- [ ] **`sitemap.xml` / `og.png` 未做**（长期 SEO 与分享缩略图资产，不在首批硬需求内）。
- [ ] **审计 r/InternetIsBeautiful 与 r/SideProject 的规则**（发帖前的 15 分钟动作）。

### 3.1.1 已完成（原计划中的前置项）

- [x] **Docker 自托管验证** —— 见 §2.6
- [x] **GitHub 仓库创建** —— 见 §2.7
- [x] **GitHub Pages 启用** —— 通过 API 设为 `build_type=workflow`，不必手动点
- [x] **CI 与部署工作流首次运行** —— 均通过

### 3.2 已解决的技术问题（记录备查）

**TypeScript 必须锁 6.x，不能用 7.x。** TS 7 的原生编译器不暴露 `astro check` 依赖的 programmatic API，装上后 `npm run typecheck` 直接报错退出。`@astrojs/check@0.9.10` 的 peer 要求是 `^5.0.0 || ^6.0.0`。

**`astro.config.mjs` 必须设 `inlineStylesheets: 'never'`。** Astro 默认会把小于 4KB 的样式表内联成 `<style>`，而我们的 CSP 是 `style-src 'self'`（不含 `unsafe-inline`）——内联样式会被浏览器拦掉，表现为**线上页面完全没有样式，而本地开发环境完全看不出问题**。

**`tsconfig.json` 不能用 `baseUrl`。** 它在 TS 6/7 已废弃，直接报 `TS5101`。不写 `baseUrl` 时 `paths` 按 tsconfig 所在目录解析，行为一致。

**`vitest.config.ts` 需要 `/// <reference types="vitest/config" />`。** 否则 `getViteConfig` 的返回类型不认识 `test` 字段，报 `TS2353`。

**性能门禁脚本的外部域名扫描只扫「会发起请求的位置」，不扫散文。** About 页说明文字里写着 `http://localhost:8080`（自托管地址），全文扫描会把它误判成外部请求。一个天天误报的门禁最终会被关掉，那比没有门禁更糟。

### 3.3 提交状态

**已提交并推送到 https://github.com/zyphraxns/plainkit**（Public）：

| 提交 | 内容 |
|---|---|
| `629d764` | `chore: initialize project skeleton` —— 70 个文件，含完整脚手架与规格文档 |
| `1738927` | `chore: keep local agent state out of the repository` —— 把 `.workbuddy/` 移出版本控制（那是本地工作状态，不该进公开仓库） |
| `2e2970c` | `fix(shell): prefix internal links with the deploy base path` —— 修复镜像站 404，并加上门禁防复发 |

`dist/`、`node_modules/`、`.workbuddy/` 均已在 `.gitignore` 中排除。仓库受版本控制文件 69 个。

---

## 4. 下一步建议

**骨架阶段到此结束。** 按 `specs/README.md` §3 的工作流，接下来是**每个工具一个对话**：

```
s8 需求澄清（产出 AC） → s9 技术方案（轻量，≤1 页）→ s10 任务规划（轻量，≤1 页）
  → s11 TDD 实现（RED → GREEN → REFACTOR） → agent-browser 浏览器验收截图
```

**开发顺序（已定）：1 → 2 → 4 → 5 → 3**（化学工具压轴，它是首批唯一有真实算法难度的）。

第一个工具：**成绩与目标分数计算器**，slug 建议 `grade-calculator`。

> **开始第一个工具前先补一件事**：`specs/DESIGN.md` 的视觉基线（配色、工具台结构、排版令牌）需要用户过目确认。50 个工具页会长在这套令牌上，现在改最便宜。
